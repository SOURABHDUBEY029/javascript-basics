let arr = [1,2,3,4,5,6];
//to insert values at end of the array//
//arr.push(7,8,9);

//to delete values from end of the array//
//arr.pop();

//to insert values at beginning of the array//
//arr.unshift(2,3,4);

//to delete values from beginning of the array//
//arr.shift();

//splice(i,2)// to delete values from index no. to the given count no.
//arr.splice(1,2);

//splice(i,2,a,b)// to replace values from index no. to the given count no.
arr.splice(1,2,7,8)
console.log(arr);
